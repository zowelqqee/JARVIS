{
  "$schema": "../node_modules/@tauri-apps/cli/schema/acl/capability.schema.json",
  "identifier": "default",
  "description": "Default capabilities for JARVIS UI",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:allow-invoke",
    "shell:allow-open",
    "shell:allow-execute"
  ]
}
